#!/bin/bash

ip_index=$(echo "$1" | grep -Eo "([0-9]{1,3}\.){3}")
echo "$ip_index"

for i in {1..255}
do
    res=$(sudo nmap -Pn "$ip_index$i")
    if [[ $res =~ "open" && $2 != "" ]];then
        echo "$res" >> "$(date)_$2"
    else
        echo "res"
    fi
done
